services:
  tbot:
    build: .
    container_name: telegram-userbot
    restart: unless-stopped
    ports:
      - "${PORT:-80}:80"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - PORT=80
