services:
  tbot:
    build: .
    container_name: telegram-userbot
    restart: unless-stopped
    ports:
      - "${PORT:-4578}:4578"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - PORT=4578
